#!/usr/bin/env node
/**
 * openspec-sdd-mcp — Phase 8
 *
 * 封装 OpenSpec CLI + Trinity CLI 操作为 MCP tools。
 * Phase 8 前 agent 直接调 CLI；Phase 8 后可选走 MCP。
 *
 * 计划 tools:
 *   - trinity_new(description)
 *   - trinity_apply(task_id)
 *   - trinity_verify(change_id)
 *   - trinity_archive(change_id)
 *   - openspec_status(change_id)
 *   - openspec_list()
 */

console.log('openspec-sdd-mcp — Phase 8, not yet implemented');
