# Use OpenSpec lifecycle through Trinity skills

SDD Harness uses OpenSpec's native change and specification lifecycle for proposal, specs, design, tasks, and archive behavior, and reaches that lifecycle through the Trinity skill architecture. Harness adds governance around entry points, context packs, domain registry checks, gates, hooks, and evidence, but it does not create a parallel spec engine or a separate task execution architecture.
