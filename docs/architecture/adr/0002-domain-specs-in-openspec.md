# Preserve accepted specifications by domain in OpenSpec

SDD Harness preserves long-lived accepted requirements in the OpenSpec `specs/` tree, organized by domain. Change directories remain the place where proposals, deltas, designs, and tasks are developed, while archived and accepted behavior is promoted into domain-specific specifications so future work can accumulate shared product and engineering memory instead of living only in run history.
