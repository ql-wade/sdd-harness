# Use a confirmed domain registry with assisted discovery

SDD Harness requires a domain registry declared during project initialization to control where long-lived OpenSpec specifications may be created or updated. Agents may propose registry entries from Understand Anything code graphs, LLMWiki organizational knowledge, and DeepWiki repository narratives, but new domains require human confirmation before they become valid specification targets.
